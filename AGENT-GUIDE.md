# 员工动机诊断｜Agent 使用须知

本须知适用于所有能读取 `SKILL.md`、处理用户材料、执行本地 Python 脚本的 Agent，不依赖单一平台。平台侧元数据只负责发现与触发，核心流程以根目录 `SKILL.md` 和本文件为准。

## 什么时候使用

- 用户要诊断某位员工（或自己）的工作动机、驱动力。
- 用户问「什么在驱动他 / 怎么激励 / 该给他分什么活」。
- 用户要做团队构成诊断，看缺哪种驱动。

不使用、明确拒绝：性格标签、绩效打分、人员排序、对不归自己带的人做背对背画像。

## 核心：双模型 + 证据链

采用两个成熟动机模型叠加，不自造分类：

- **麦克利兰三需要**（成就 / 亲和 / 权力）→ 判断被什么驱动（方向）。
- **自我决定理论 SDT**（自主 / 胜任 / 归属）→ 判断动机质量与是否被满足（状态）。

每条结论都必须回溯到具体飞书证据，格式：维度 → 支撑证据（哪个资产的哪条行为）→ 推断逻辑 → 置信度 + 需确认提示。**行为不完全等于动机**，结论是推断。

首次使用时，先用大白话向用户解释六个维度大概指什么（见 `references/motivation-model.md` 或 SKILL.md 的科普块），别直接甩术语。

## 数据来源

- 主依据（工作产出）：OKR、文档、周会/会议表现、员工与 AI Agent 的交流内容（codex / claude / 豆包企业版等，agent-agnostic）。
- 辅助：对话消息——仅辅助，且他评模式下必须对本人透明，不做背后扒消息。

## 输出结构：先总结，再建议

固定为：**总结**（主导驱动的驱动画像 + SDT 动机状态小结）→ 逐维度证据链 → **建议**（按 audience 的视角）+ 需确认提示。

## 视角切换（重要）

- **audience=self（员工自测）**：站在员工视角，给「你的主导动机、当前满足状态、未来该争取什么、补哪一块」。
- **audience=manager（Leader/HR 他评）**：站在管理视角，给「该成员被什么驱动、该分什么活、怎么激励、团队怎么补位」。

CLI 显式 `--audience` 优先；否则读输入里的 `context.audience`。

## 执行顺序

### 1. 确认模式与边界
自测还是他评。他评时确认使用者是该成员直接管理者、成员知情。要对不归自己带的人做背后分析，停止。

### 2. 收集飞书证据（只读、脱敏）
先核验身份 `lark-cli auth status --json --verify`。按 `references/feishu-evidence.md` 从各资产梳理证据，映射到六维度，整理成脱敏输入（`references/input-schema.md`）。

### 3. 诊断
```bash
python3 scripts/diagnose.py --input <evidence.json> --audience self --format markdown
python3 scripts/diagnose.py --input <evidence.json> --audience manager --format markdown
```

### 4. 团队诊断（可选）
```bash
python3 scripts/diagnose.py --team <team.json> --format markdown
```

### 5. 落到行动
按 `references/driver-actions.md` 给驱动对齐的授权与发展建议，落地前先和本人确认。

## 停止规则

- 要做背对背画像、性格标签、绩效打分、人员排序时，停止并说明边界。
- 证据不足时只给「假设 + 待核实」，不下定论。
- 消息类证据仅辅助且他评须透明。
- 结论用于发展性对话与授权，不进考核。

## 边界一句话

飞书证据是帮你更懂这个人，不是让系统替你判定这个人。最终解释权留给本人。
